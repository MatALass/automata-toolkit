# Limitations

- No epsilon-transition support in the current version.
- Parser supports a single explicit text format only.
- DOT export writes `.dot` text but does not invoke Graphviz automatically.
- Minimization assumes a deterministic complete automaton and internally enforces those prerequisites.
- No batch HTML reporting yet.
